apiVersion: 1

providers:
  - name: Default
    folder: gnp-stack
    type: file
    options:
      path: /var/lib/grafana/dashboards/gnp-stack

  - name: XRd
    folder: XRd SR Topology
    type: file
    options:
      path: /var/lib/grafana/dashboards/xrd
